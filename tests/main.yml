---
- hosts: localhost
  gather_facts: no
  roles:
    - name: ../..
  tasks:
    - file:
        dest: /tmp/foo
        state: touch

    - file:
        dest: /tmp/bar
        state: touch

    - debug: var=file_paths
